# Compiled binaries
*.exe
*.exe~
/gobot.exe
resource.syso

# Test cache
*.test

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Secrets — never commit
.env
config.local.json

# Strategic Private Data (legacy — superseded by .crucible/ per Crucible dog-food)
.private/
.private.archive/
.gemini/
.agent-workspaces/

# Crucible framework integration (.crucible/ tracks config + overrides only;
# data dirs are excluded by the nested .crucible/.gitignore)
# Do NOT add `.crucible/` here — it must be partially tracked.

# Vendor dependencies
vendor/

# Gemini CLI task handoff file (overwritten each task, not source)
_current_task.md
.aider*

# Personal planning notes (Claude conversations)
CLAUDE.md
GEMINI.md
AGENTS.md
opencode.json

# Backlog lives in .crucible/backlog/ (gitignored as data) — root copy must never exist
BACKLOG.md

# Claude local settings
.claude/
.claudeignore.txt

# Dev-only scripts (live locally, not published)
scripts/profile.ps1
scripts/profile.sh

# Temporary/Generated Files
_trash/
bin/
artifacts/
bench.txt
coverage-windows*
coverage_baseline
c.out
s.out
v.out
*.out
coverage
coverage/
coverage.out
package-lock.json

# GitHub API research artifacts
oc_go.json
oc_main.json
# Per-worktree Go and Lint caches
.go-build-cache/
.golangci-lint-cache/

# Temporary files
.tmp/

# Crucible installed docs whose filenames collide with root personal-note
# ignores on case-insensitive filesystems.
!.crucible/docs/orchestrators/claude.md
!.crucible/docs/orchestrators/gemini.md
