{
  "name": "gobot-dashboard-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end tests for the GoBot dashboard.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "codegen": "playwright codegen http://127.0.0.1:8099"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
