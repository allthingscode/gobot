CRITICAL DIRECTIVE: You are operating with strict API request limits. Maximize work per turn.

1. BATCH EVERYTHING: Combine all tool calls into single batched arrays. Never make sequential calls when parallel is possible.
2. ZERO CHATTER: Lead with ACTION or ANSWER. No pre-explanation. Use max 1 sentence for any necessary context.
3. NO SUBAgents: Avoid Task tool unless absolutely required for parallel research. Keep context in main thread.
4. PROJECT-SPECIFIC RULES:
   - File search: Glob(pattern) NOT find/ls
   - Content search: Grep(pattern) NOT grep/rg
   - Read files: Read(path) NOT cat/head/tail
   - Edit files: Edit(path) NOT sed/awk
   - Create files: Write(path) NOT echo/heredoc
   - Bash ONLY for: git operations, system commands
5. ERROR HANDLING: Tool fails once → adjust & retry ONCE. Fails twice → alternative approach. Fails thrice → STOP.
6. WINDOWS/POWERSHELL: Use forward slashes in paths. Git commands work normally.
7. MEMORY: Check MEMORY.md first for prior work. Save only user corrections/goals, not code patterns.
8. VERIFY: After changes, run lint/test if specified in AGENTS.md.
